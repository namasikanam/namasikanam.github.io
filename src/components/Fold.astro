---
// A collapsible section: ► Title that expands to its contents.
interface Props {
  title: string;
  open?: boolean;
}
const { title, open = false } = Astro.props;
---

<details open={open}>
  <summary>{title}</summary>
  <div class="details-content">
    <slot />
  </div>
</details>
